 ---
interface Props {
	title: string;
	body: string;
	link: string;
}

const { link, title, body } = Astro.props;
---
 
 
 
<div class="kard">
      <h2 class="card-title">{title}</h2>
      <a href={link}>{title}</a>
      <p class="card-body">{body}</p>
</div>


    <style>
.kard {
  background-color: #292929;
  border-radius: 10px;
  padding: 20px;
  margin: 10px;
}

.card-title {
  font-size: 20px;
  font-weight: bold;
  margin-bottom: 10px;
  color: rgb(255, 255, 255);
}

.card-link {
  color: #ffffff;
  text-decoration: none;
}

.card-link:hover {
  text-decoration: underline;
}

.card-body {
  font-size: 16px;
  line-height: 1.5;
}

    </style>